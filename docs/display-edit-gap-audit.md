# 表示・編集ギャップ監査（2026-08-11）

発端：条件明細の「相手方」がブランクのまま直せない、という指摘。
「**表示はあるのに編集手段がない**」「**参照・書込ロジックが欠けていて常にブランク**」を
全画面×全フィールドで棚卸しした（読取専用の監査エージェント3系統で全クライアント画面 →
ルート → SQL → 書込文の全数トレース）。本書はその統合結果と、第1波で入れた修正、残課題の優先順位。

## 根本構造（ほぼすべての指摘の源泉）

1. **`condition_lines`（条件明細）は全コードで追記専用だった。** UPDATE 文が1つも存在せず、
   取込・アウト条件で書いた料率・金額・相手方等は以後永久に不変。データ品質センターは
   「未リンク条件明細（severity: high）」を検出するのに修復手段がゼロだった。
2. **V1 遺産データの NULL FK**（相手方・作品・文書）が LEFT JOIN で無言の「—」になり、
   未設定なのか該当なしなのか判別できなかった。
3. **API は対応済みなのに UI に入力欄が無い**フィールド群（案件担当者・タスク担当・
   作品の権利者・契約マスタの更新通告/アラート/見直し期日）。
4. **`parent_license_condition_id` は全 INSERT に含まれない** → 請求の「未リンク」バッジ・
   債権マップの上流分配・契約チェックの当該指摘は V2 データでは構造的に常時発火。

## 第1波で修正済み（このコミット群）

| # | 修正 | 種別 |
|---|---|---|
| 1 | **条件明細の相手方補修**：詳細画面に「設定する/変更」→ 取引先マスタから選択（PATCH /condition-lines/:id/counterparty・guarded・admin/legal）。一覧・詳細のブランクは「未設定」バッジ＋説明表示に。条件行ID も詳細に表示（受領記録フォームで必要なのにどこにも出ていなかった） | 新機能（guarded） |
| 2 | **作品画面の素材編集で備考が消えるバグ**：読取APIが remarks を返さず、編集保存で常に null 送信 → 既存備考を消していた。読取に追加＋プリフィル。あわせて「種別」セレクトが保存されないのに編集可能に見えた問題を修正（変更不可表示） | **データ破壊バグ** |
| 3 | **数量ベース受領の¥0黙殺**：条件明細に単価未設定のまま数量ベースで受領記録すると ¥0 を成功として保存していた → 422 で理由を返して拒否 | **金額データ破壊バグ** |
| 4 | **アウト条件保存が source_work_id / is_inbound / flow_direction を書かない**：V2作成行が請求ダッシュボードの作品列・債権マップ集計から漏れていた → INSERT に追加 | 集計欠落バグ |
| 5 | **案件担当者・タスク担当のピッカー**（表示は「担当者未設定」なのに設定手段が無かった。API対応済み）。Slack 担当者メンションもこれで機能するようになる | UIギャップ |
| 6 | **作品の権利者（取引先）ピッカー**（API対応済み・UI無し） | UIギャップ |
| 7 | **作品ステータス編集**（企画中/制作中/発売済み。従来は契約取込時のみ設定可）。grant 012 のテーブルレベル UPDATE で追加グラント不要 | UIギャップ |
| 8 | **契約マスタ：更新通告月数・アラート月数・見直し期日の入力欄**（PATCH対応済みだが入力欄が無かった） | UIギャップ |
| 9 | **台帳詳細の null 隠し**：null項目が行ごと消えて「未設定」と「該当なし」が判別不能 → 「未設定」表示に。原作IP行には「V1由来・読み取り専用」の説明を表示 | 表示改善 |
| 10 | **データ品質センターの「開く」が行IDを捨てていた**：条件明細・作品のドリルが該当行の詳細を直接開くように | 導線修正 |
| 11 | **条件明細詳細の生コード表示**（non_exclusive 等）をラベル化 | 表示改善 |
| 12 | **送信記録に件名欄**（保存先はあったが入力欄が無かった） | UIギャップ |

相手方補修の点火は**追加グラント不要**（名寄せ用 grant 018 の列レベル UPDATE
`counterparty_vendor_id` を再利用）。デプロイフラグのみ：
`_CONDITION_LINE_REPAIR_ENABLED=true`＋`_CONFIRM_CONDITION_LINE_REPAIR=CONDITION_LINE_REPAIR_LEGALBRIDGE_VALIDATION_ONLY`
＋`_WRITE_SCOPES` 末尾に `,condition-repair`。

## 残課題（優先順位つきバックログ）

### P1（金額の正しさに直結・要設計判断）
- **条件明細の本体編集**（料率・金額・MG・期間・向き等）：¥・料率の入力ミスが受領計算に永久伝播する。
  monetary データのため、変更履歴（誰がいつ何を）を持つ guarded PATCH として設計すべき。
  条件明細詳細画面が自然な置き場。要：condition_lines の列レベル UPDATE グラント追加＋監査ログ。
- **parent_license_condition_id（上流ライセンス条件リンク）**：書き手ゼロのため分配・債権マップ上流が
  構造的に空。アウト条件追記フォームに「根拠イン条件」選択（UIは既に半分ある）＋既存行の後付けリンク機能。
- **受領記録の訂正 UI**：PUT /condition-receipts/:id は実装・テスト済みだが呼ぶ画面が無い。
  実受領額・期間の入力ミスが現状取り消せない。請求ダッシュボードに編集ボタンを追加するだけで機能する。
- **payments.tax_rate**：受領同期の INSERT が書かないため支払報告書が全行 10% 既定で表示（既定である表示も無し）。

### P2（業務データの完全性）
- **確定時に documents.matter_id を自動設定**（issue_key から案件解決）。現状は全確定文書が案件から孤児。
- **文書の件名・相手方の後付け修正**（form_data 不変・過去文書取込は `{}`）：メタデータ修正 PATCH。
- **取引先の代表者・口座情報**：V2 に書込手段ゼロだが帳票（振込先等）へ差し込まれる。
  機微情報のため要権限設計（admin限定 guarded）。
- **素材・権利ソース・系譜(work_relations)の削除**：追加のみで誤登録が消せない。
- **document_sends の訂正/削除**：誤記録が永久に残る。

### P3（表示・導線）
- 文書台帳の明細（items 等の配列項目）が詳細で丸ごと非表示／40項目超の無言切り捨て。
- 文書一覧の状態バッジが reissued/superseded を「確定済み」と表示。
- CloudSign/Gmail 送信履歴が DB にあるのに読むルートが無い（リロードで消える）。
- 下書き一覧の文書番号列（常に確定前）・検索プレースホルダの文書番号言及。
- matter_issues の summary_snapshot/note・document_drafts.document_number 等、書き手のない列の整理。
- ContractIntakeWorkspace.tsx（1,079行）はどこからも import されない死コード → 削除候補。

## 監査の生データ
3系統の詳細（画面×フィールド×判定の全表）は本ドキュメントの元となったセッション記録にあり。
要点はすべて上記に反映済み。
